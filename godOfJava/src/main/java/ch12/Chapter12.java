package ch12;
/* Object 클래스 */
public class Chapter12 {

    /* 어떤 클래스가 아무런 상속을 받지 않으면, 기본적으로 Object클래스를 확장한다. */

    /* Object 클래스에서 제공하는 메소드들의 종류
    *  1. 객체를 처리하기 위한 메소드 : toString(), equals(), hashCode(), getClass(), clone(), finalize()
    *  2. 쓰레드를 위한 메소드 */
}
